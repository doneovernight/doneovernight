# Manifests

Reserved for machine-readable sprite metadata, release manifests, and checksums.

The production JSON schema will be locked during Phase 2 and must include frame rectangles, ordered animations, per-frame durations, loop behavior, anchors, ground line, palette, version, exports, checksums, and approval metadata.

Timing must never be inferred from filenames or a fixed FPS.
